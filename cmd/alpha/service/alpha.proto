syntax = "proto3";

package alpha;

option go_package = "service";

service AlphaService {
  rpc Get(GetRequest) returns (GetResponse);
}

message GetRequest{
  string data = 1;
}

message GetResponse {
  string data = 1;
}